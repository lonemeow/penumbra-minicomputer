#	$NetBSD$
#
# MINIMAL -- bare-minimum Penumbra kernel config for initial build testing.
# Not expected to produce a bootable kernel yet.
#

include		"arch/penumbra/conf/std.penumbra"

maxusers	2

# Build at -O0 for now: our LLVM backend doesn't yet implement
# analyzeBranch/insertBranch needed by -O2 machine passes.
makeoptions	COPTS="-O0"

# Kernel options
#options	DDB			# kernel debugger (needs MD hooks)

# File systems (minimal set)
file-system	FFS			# Berkeley Fast File System

# Networking (minimal)
options		INET			# IP

# System bus
mainbus0	at root
cpu0		at mainbus?

# Console
options		CONSDEV_OVERRIDE="\"uart\""

# Pseudo-devices
pseudo-device	loop			# loopback interface
pseudo-device	pty			# pseudo-terminals
pseudo-device	ksyms			# /dev/ksyms

config		netbsd root on ? type ?
